#include <bits/stdc++.h>

using namespace std;

int main()
{
    long a[10],sum=0;
    for (int i=0;i<5;i++)
    {
        cin>>a[i];
        //cout<<a[i]<<endl;
        sum+=a[i];
    }
    sort(a,a+5);
    cout<<sum-a[4]<<" "<<sum-a[0];
    return 0;
}
